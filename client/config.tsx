interface IConfig {
  app: {
    title: string,
    description: string
  }
}

const config: IConfig = {
  app: {
    title: 'AV eCommerce',
    description: 'Universal React App'
  }
};

export default config;